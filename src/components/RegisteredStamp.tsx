export function RegisteredStamp() {
  return (
    <span
      aria-hidden="true"
      className="absolute -right-2 -top-2 animate-stamp-in rounded-full border-2 border-emerald-500 bg-white px-2 py-0.5 text-[10px] font-bold uppercase tracking-wide text-emerald-600 shadow dark:bg-slate-900"
    >
      Registered
    </span>
  );
}
